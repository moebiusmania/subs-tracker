{{# value and description are Alpine expressions, evaluated client side #}}
<div class="stat">
  <div class="stat-title">{{ title }}</div>
  <div class="stat-value" x-text="{{ value }}"></div>
  <div class="stat-desc" x-text="{{ description }}"></div>
</div>
